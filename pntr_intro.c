#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int main()
{
	char *mem_alloc;
	if (mem_alloc == NULL)
	printf("countn't allocate memory");
	else{
	
	strcpy(mem_alloc,"manjitha");}
	printf("memory allocated is :%s\n",mem_alloc);
	free(mem_alloc);

	
	
	
	
}
